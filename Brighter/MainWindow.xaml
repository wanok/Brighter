<Window x:Class="Brighter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow">
    <Grid Name="MainGrid">
        <Grid.Background>
            <StaticResource ResourceKey="BackgroundColor" />
        </Grid.Background>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition MinWidth="200"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <ScrollViewer Name="Viewport" Grid.Row="1" Grid.Column="0" Grid.RowSpan="3" 
                      HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Canvas Name="CanvasHolder" Background="Gray">
                
            </Canvas>
        </ScrollViewer>
        <GridSplitter Grid.Column="0" Grid.Row="1" Grid.RowSpan="3" Width="3"/>
    </Grid>
</Window>
