<UserControl x:Class="Brighter.LayersPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="0">V</Label>
        <Label Grid.Row="0" Grid.Column="1">Prev</Label>
        <Label Grid.Row="0" Grid.Column="2">Name</Label>
        <ScrollViewer Grid.Column="0" Grid.ColumnSpan="3"
                      Grid.Row="1" UseLayoutRounding="True">
            <StackPanel Name="LayersGroup"></StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
